/*import React from "react";
import Carousel from "react-bootstrap/Carousel";

const Testimonials = () => {
  const reviews = [
    {
      id: 1,
      text: "This is an amazing product! Highly recommend it to everyone.",
      author: "John Doe",
    },
    {
      id: 2,
      text: "Excellent service and support. Five stars!",
      author: "Jane Smith",
    },
    {
      id: 3,
      text: "A remarkable experience, will definitely come back for more.",
      author: "Samuel Green",
    },
  ];

  return (
    <section id="testimonials" className="testimonials">
      <h2 className="section-title">Testimonials</h2>
      <Carousel
        className="testimonial-carousel"
        indicators={true}
        nextIcon={<span className="carousel-control-next-icon" />}
        prevIcon={<span className="carousel-control-prev-icon" />}
      >
        {reviews.map((review) => (
          <Carousel.Item key={review.id}>
            <div className="carousel-content">
              <p className="testimonial-text">{review.text}</p>
              <footer className="testimonial-author">- {review.author}</footer>
            </div>
          </Carousel.Item>
        ))}
      </Carousel>
    </section>
  );
};

export default Testimonials;

*/
